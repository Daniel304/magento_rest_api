require 'spec_helper'

describe MagentoRestApi do
  it 'has a version number' do
    expect(MagentoRestApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
